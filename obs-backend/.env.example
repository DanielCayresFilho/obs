# ============================================
# Backend Environment Variables (Coolify)
# ============================================

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# No Coolify: Use a URL do serviço PostgreSQL interno
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/obs_db?schema=public

# JWT Configuration
# IMPORTANTE: Gere um segredo forte para produção!
# Comando: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=1d

# Application Configuration
# NODE_ENV=production para deploy no Coolify
NODE_ENV=development
PORT=3000

# CORS Configuration (OBRIGATÓRIO para produção)
# Lista separada por vírgula dos domínios que podem acessar a API
# Exemplo: https://studioobscura.com.br,https://www.studioobscura.com.br
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
